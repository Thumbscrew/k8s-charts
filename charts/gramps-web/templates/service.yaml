apiVersion: v1
kind: Service
metadata:
  name: {{ include "gramps-web.fullname" . }}
  labels:
    {{- include "gramps-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gramps-web.selectorLabels" . | nindent 4 }}
